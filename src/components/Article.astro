---
/**
 * A generic article with a heading (use slot="heading") and content.
 */

const { class: className, ...rest } = Astro.props;
---
<article class={className} {...rest}>
    <slot name="heading" />

    <div class="mt-5">
        <slot />
    </div>
</article>